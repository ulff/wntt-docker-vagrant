# behat.yml

default:
    suites:
        web_features:
            paths:    [ %paths.base%/features ]
            contexts: [ AdminContext ]

    extensions:
            Behat\MinkExtension:
                  base_url: "http://127.0.0.1/"
                  sessions:
                    default:
                      goutte: ~
